# This must exactly match one of the three profile names below.
default = "Balanced"

# Profiles may be quantizations of one model or completely different models.
# llama.cpp settings are discovered from each service. A coding agent can add
# and tune a new service when you download another GGUF model.
[[profiles]]
name = "Quick"
service = "local-ai-quick.service"
# summary = "Optional human-readable description"
# endpoint = "http://127.0.0.1:8080/v1"

[[profiles]]
name = "Balanced"
service = "local-ai-balanced.service"

[[profiles]]
name = "Quality"
service = "local-ai-quality.service"
